#include <iostream>
#include <string>
#include <stdio.h>
#include <math.h>
using namespace std;

int main(int argc, char* argv[])
{
	double a, b, c;
	scanf("%f %f %f", &a, &b, &c);

	double left = sqrt(a) * sqrt(b);
	double right = c-a-b;

	return 0;
}



